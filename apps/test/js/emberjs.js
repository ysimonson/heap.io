function testEmber() {
    module("Ember.js adapter");

    /*var emberReady = function() {
        
    };

    var App = Em.Application.create({
        ready: emberReady,
        store: DS.HeapIOAdapter.create()
    });

    var Person = DS.Model.extend({
        first: DS.attr("string"),
        last: DS.attr("string"),
        age: DS.attr("number")
    });*/
}